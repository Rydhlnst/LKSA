import Link from "next/link";
import { ArrowRight, Images } from "lucide-react";
import { getSiteContent } from "@/lib/content-store";
import { PublicShell } from "@/components/site/public-shell";

export default async function GalleryPage() { const content = await getSiteContent(); const albums = content.galleries.filter((album) => album.visible); return <PublicShell content={content}><section className="bg-[#f8fafc] py-16 md:py-24"><div className="site-container"><span className="eyebrow">Dokumentasi</span><h1 className="mt-5 font-heading text-4xl font-bold text-ink md:text-6xl">Galeri Kegiatan</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-muted">Momen-momen berharga dan dokumentasi aktivitas di lingkungan panti asuhan kami.</p></div></section><section className="site-container py-16 md:py-24">{albums.length ? <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{albums.map((album) => <Link key={album.id} href={`/galeri/${album.slug}`} className="group overflow-hidden rounded-2xl border border-line bg-white shadow-subtle"><div className="aspect-[16/10] bg-cover bg-center transition duration-500 group-hover:scale-105" style={{ backgroundImage: `url(${album.coverUrl})` }} /><div className="flex items-center justify-between p-5"><div><h2 className="font-heading text-lg font-bold text-ink">{album.title}</h2><p className="mt-1 text-sm text-muted">{album.date}</p></div><ArrowRight className="h-5 w-5 text-orange" /></div></Link>)}</div> : <div className="empty-state"><Images className="mx-auto h-8 w-8 text-orange" /><p className="mt-3 font-semibold text-ink">Belum ada album galeri yang tersedia.</p><p className="mt-1 text-sm text-muted">Album yang ditambahkan dari CMS akan muncul di halaman ini.</p></div>}</section></PublicShell>; }
