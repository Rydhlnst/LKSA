import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { getSiteContent } from "@/lib/content-store";
import { PublicShell } from "@/components/site/public-shell";

export default async function NewsPage() { const content = await getSiteContent(); const articles = content.articles.filter((article) => article.status === "published").sort((a, b) => b.publishDate.localeCompare(a.publishDate)); return <PublicShell content={content}><section className="bg-[#f8fafc] py-16 md:py-24"><div className="site-container"><span className="eyebrow">Kabar Terbaru</span><h1 className="mt-5 font-heading text-4xl font-bold text-ink md:text-6xl">Berita & Artikel</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-muted">Ikuti terus perkembangan, kegiatan, dan cerita dari kami.</p></div></section><section className="site-container py-16 md:py-24"><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{articles.map((article) => <article key={article.id} className="overflow-hidden rounded-2xl border border-line bg-white shadow-subtle"><div className="aspect-[16/10]" style={{ backgroundImage: `url(${article.coverUrl})`, backgroundSize: "cover", backgroundPosition: "center" }} /><div className="p-6"><p className="text-xs font-bold uppercase tracking-wider text-orange">{article.publishDate}</p><h2 className="mt-3 font-heading text-xl font-bold leading-snug text-ink">{article.title}</h2><p className="mt-3 text-sm leading-6 text-muted">{article.excerpt}</p><Link href={`/berita/${article.slug}`} className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-ink hover:text-orange">Baca Artikel <ArrowRight className="h-4 w-4" /></Link></div></article>)}</div>{!articles.length && <div className="empty-state">Belum ada berita yang dipublikasikan.</div>}</section></PublicShell>; }
