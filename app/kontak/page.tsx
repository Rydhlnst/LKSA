import { Mail, MapPin, MessageCircle, Phone } from "lucide-react";
import { getSiteContent } from "@/lib/content-store";
import { PublicShell } from "@/components/site/public-shell";

export default async function ContactPage() { const content = await getSiteContent(); const { settings } = content; return <PublicShell content={content}><section className="bg-[#f8fafc] py-16 md:py-24"><div className="site-container"><span className="eyebrow">Hubungi Kami</span><h1 className="mt-5 font-heading text-4xl font-bold text-ink md:text-6xl">Kami siap mendengar dari Anda</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-muted">Silakan hubungi kami untuk informasi lebih lanjut mengenai panti asuhan, program, atau donasi.</p></div></section><section className="site-container grid gap-8 py-16 lg:grid-cols-[.8fr_1.2fr] lg:py-24"><div className="grid gap-4"><a href={settings.mapUrl} className="contact-card"><MapPin className="h-5 w-5 text-orange" /><span><b>Alamat Lengkap</b><small>{settings.address}</small></span></a><a href={`tel:${settings.phone}`} className="contact-card"><Phone className="h-5 w-5 text-orange" /><span><b>Telepon</b><small>{settings.phone}</small></span></a><a href={`https://wa.me/${settings.whatsappNumber}`} className="contact-card"><MessageCircle className="h-5 w-5 text-green" /><span><b>WhatsApp</b><small>{settings.whatsappNumber}</small></span></a><a href={`mailto:${settings.email}`} className="contact-card"><Mail className="h-5 w-5 text-orange" /><span><b>Email</b><small>{settings.email}</small></span></a></div><div className="min-h-[360px] overflow-hidden rounded-3xl border border-line bg-[#edf2f7] shadow-subtle"><iframe title="Peta lokasi LKSA" src={`${settings.mapUrl}&output=embed`} className="h-full min-h-[360px] w-full border-0" loading="lazy" /></div></section></PublicShell>; }
