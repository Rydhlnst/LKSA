import { requireAdmin } from "@/lib/auth";
import { getSiteContent } from "@/lib/content-store";
import { AdminHeader, AdminShell } from "@/components/admin/admin-shell";
import { updateSettingsAction } from "@/app/admin/actions";

const input = "mt-2 w-full rounded-xl border border-line bg-white px-3 py-2.5 text-sm text-ink shadow-sm";

export default async function SettingsAdminPage() { await requireAdmin(); const { settings } = await getSiteContent(); return <AdminShell><AdminHeader eyebrow="Instansi & komunikasi" title="Pengaturan website" description="Atur identitas, kontak publik, tautan peta, dan pesan WhatsApp yang muncul di seluruh halaman." /><div className="max-w-5xl p-5 md:p-10"><form action={updateSettingsAction} className="grid gap-6 rounded-2xl border border-line bg-white p-6 shadow-subtle md:p-8"><div className="grid gap-5 md:grid-cols-2"><label className="text-sm font-semibold">Nama organisasi<input className={input} name="organizationName" defaultValue={settings.organizationName} required /></label><label className="text-sm font-semibold">Nama pendek<input className={input} name="shortName" defaultValue={settings.shortName} required /></label><label className="text-sm font-semibold md:col-span-2">Alamat<textarea className={input} name="address" defaultValue={settings.address} rows={3} required /></label><label className="text-sm font-semibold">Telepon<input className={input} name="phone" defaultValue={settings.phone} required /></label><label className="text-sm font-semibold">Email<input className={input} name="email" type="email" defaultValue={settings.email} required /></label><label className="text-sm font-semibold md:col-span-2">URL peta<input className={input} name="mapUrl" type="url" defaultValue={settings.mapUrl} required /></label></div><div className="border-t border-line pt-6"><h2 className="font-heading text-xl font-bold">WhatsApp Widget</h2><div className="mt-5 grid gap-5 md:grid-cols-2"><label className="text-sm font-semibold">Nomor WhatsApp<input className={input} name="whatsappNumber" defaultValue={settings.whatsappNumber} required /></label><label className="text-sm font-semibold">Nama admin<input className={input} name="whatsappAgentName" defaultValue={settings.whatsappAgentName} required /></label><label className="text-sm font-semibold md:col-span-2">Status respons<input className={input} name="whatsappResponseTime" defaultValue={settings.whatsappResponseTime} required /></label><label className="text-sm font-semibold md:col-span-2">Pesan pembuka<textarea className={input} name="whatsappGreeting" defaultValue={settings.whatsappGreeting} rows={3} required /></label><label className="text-sm font-semibold md:col-span-2">Pesan prefilled<textarea className={input} name="whatsappMessage" defaultValue={settings.whatsappMessage} rows={3} required /></label></div></div><button className="button-primary w-fit">Simpan Pengaturan</button></form></div></AdminShell>; }
